{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@baerly/adapter-cloudflare": "^0.1.0",
    "@baerly/server": "^0.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "wrangler": "^4.10.0"
  }
}
