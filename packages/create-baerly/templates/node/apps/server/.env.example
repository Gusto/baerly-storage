# Storage credentials (S3 / R2 / GCS via S3-compat / Minio)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
BUCKET=
# Optional override for non-AWS targets:
# S3_ENDPOINT=https://<account>.r2.cloudflarestorage.com

# Auth — choose ONE
# Production: JWKS-backed JWT.
# JWKS_URL=https://login.example.com/.well-known/jwks.json
# JWT_ISSUER=https://login.example.com/
# JWT_AUDIENCE=https://api.example.com/
# Development: shared secret.
SHARED_SECRET=

# Maintenance — optional. When set, the server schedules hourly
# compaction+GC for this current.json key.
# MAINTENANCE_KEY=app/{{appName}}/tenant/{{tenant}}/manifests/{{appName}}/current.json

# Server
PORT=8080
TENANT={{tenant}}
