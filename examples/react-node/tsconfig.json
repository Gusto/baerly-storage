// Stub — TS picks up the referenced projects below.
// `tsconfig.app.json` covers the SPA (DOM); `tsconfig.server.json`
// covers the Node entry (`server.ts`). Tests at the repo root match
// `vitest.config.ts:test.include` (e.g. `*.test.ts`); they don't
// belong to any tsconfig — vitest type-checks them via its own pipe.
{
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }, { "path": "./tsconfig.server.json" }]
}
