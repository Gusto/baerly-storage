{
  "name": "minimal-cloudflare-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "baerly dev",
    "dev:wrangler": "baerly dev --wrangler",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@baerly/adapter-cloudflare": "workspace:*",
    "@baerly/server": "workspace:*"
  },
  "devDependencies": {
    "@baerly/cli": "workspace:*",
    "@cloudflare/workers-types": "^4",
    "typescript": "^5.6.0",
    "wrangler": "^4.10.0"
  }
}
