# Claude Things
.claude/reviews/
.claude/research/
.claude/worktrees/
.claude/settings.local.json

# Dependencies
node_modules/
.pnpm-store/
.pnpm-debug.log*

# Build output
dist/
build/
*.tsbuildinfo

# pnpm pack output (see ticket 04 / staged tarball install path)
*.tgz

# Test & coverage
coverage/
*.lcov
.nyc_output/

# Bench harness outputs (per-run JSON + CSV)
bench/results/*
!bench/results/.gitkeep
!bench/results/load/
bench/results/load/*
!bench/results/load/.gitkeep

# Bench load-harness fixtures (raw corpora; only calibration.json
# under presets/ is checked in — see scripts/fetch-bench-fixtures.sh).
bench/fixtures/

# Vite / Vitest / Rolldown caches
.vite/
.vitest/
.rolldown/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment & secrets
.env
.env.*
!.env.example
credentials/

# Editor & IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Project-specific
docs/whisper/

# Day-one gate temp scaffolds
.baerly-day-one/

# Scaffolding-eval per-trial outputs (large + leaky; score.md and
# metrics.json are intentionally NOT ignored).
eval/runs/**/transcript.jsonl
eval/runs/**/final-state.diff
eval/runs/**/stderr.log
eval/runs/**/env.txt
