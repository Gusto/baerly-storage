---
title: Pricing log
audience: operator
summary: Append-only audit of price and cap changes.
last-reviewed: 2026-06-23
tags: [audit, pricing]
related: [cost-model.md]
---

# Pricing log

One line per price-or-cap change that affects baerly-storage users.
Append-only. Read top-to-bottom for the rate-of-change of what costs
money or graduates the user out.

This is a **trust artifact**, not a marketing page. If baerly-storage's
ceiling shifts, the new ceiling lands here on the same day it lands
in code. If an upstream provider changes a billable rate, the
multiplier on baerly-storage's cost model lands here on the same day the
cost model gets the new number.

Cost model: [cost-model.md](cost-model.md).

| Date | Change | Workload-shape impact |
|---|---|---|
| 2026-05-11 | **Initial workload ceiling published.** ~30 logical writes/min/collection sustained; ~10 GB/tenant total; ~100 collections/tenant fan-out. Above any one of these: graduate to D1 / Postgres. Platform-independent — same ceiling on Cloudflare Workers, self-hosted Node, AWS Lambda. *(Clarified 2026-06-14 — see below: this is the protocol-level **workload** ceiling, which is host-independent; the host-dependent number is the **maintenance** ceiling, a separate value.)* | This is the design ceiling, not a quota. Workloads inside the ceiling sit in Cloudflare's free tier for storage (10 GB-mo free) and stay below ~$20/mo total at the M-size operating point. Workloads above the ceiling start hitting R2 Class A op cost (the protocol's 3× write-amplification compounds), Worker CPU-ms overage, and per-collection log-fold latency. |
| 2026-05-27 | **Positioning.** Reframed "cost is not the moat" thesis bullet + cost-model.md to a 4-axis CTO comparison (per-write unit cost, idle × portfolio, availability, switching cost). Added "At the audience operating point" table grading cost at idle × N=30 portfolio (baerly-storage $0–$5, Supabase Pro $750, Neon Launch $150). M-size alt-DB table preserved as graduation-cliff reference. | No rate change. Docs-only. Workload ceiling and per-line-item rates unchanged. |
| 2026-06-12 | **Provider rate re-check.** Official Cloudflare R2, Workers pricing, and Workers limits pages still match the published cost model: R2 Standard storage $0.015/GB-mo, Class A $4.50/1M, Class B $0.36/1M, 10 GB / 1M Class A / 10M Class B free tier; Workers Paid $5 floor, 10M requests + 30M CPU-ms included, $0.30/1M requests and $0.02/1M CPU-ms after that; Workers Free CPU 10 ms, Paid default 30 s / max 5 min. | No rate change. Documentation review only. |
| 2026-06-13 | **Competitor comparison re-check + N=30 portfolio re-expression.** Refreshed Neon, Supabase, and Firebase wording to match current official pricing posture: Neon Launch is usage-based with no monthly minimum, Supabase Pro bills per project (own always-on Postgres compute), and Firebase Spark is a per-project no-cost quota rather than a pause promise. The N=30 idle-portfolio table's competitor cells were re-expressed onto a single consistent **per-app basis × 30 estimate** form: the prior hard totals from the 2026-05-27 entry (Supabase Pro ~$750, Neon Launch ~$150) are **restated as derivations** — Supabase Pro ≈ $25/app × 30 ≈ ~$750/mo *(est.)*, Neon Launch ≈ $5/app × 30 ≈ ~$150/mo *(est.)* — so no usage-based total is asserted as a hard fact and the portfolio multiplier (×30) stays visible in every cell. | No baerly-storage rate or ceiling change. Keeps the N=30 idle-portfolio argument internally consistent (one basis per column) while being honest that variable-priced competitor totals are estimates, not quotes. |
| 2026-06-14 | **Ceiling clarification + CF limit correction (truth-in-docs).** The 2026-05-11 "same ceiling on CF / Node / Lambda" line conflated two distinct ceilings. The *workload* ceiling (~10 GB/tenant, ~30 writes/min/collection, ~100 collections/tenant) is a protocol-level **target** and is host-independent by design — it falls out of S3/R2 CAS-livelock, storage, and fan-out, not the runtime. The *maintenance* ceiling — how large a snapshot a host can fold before compaction defers — **is** host-dependent and not yet fully benched: CF-free is CPU-bound (10 ms caps fold size), CF-paid relaxes CPU (30 s default / 5 min max) but is memory-bound, Node is bounded by host RAM. Corrected a related CF fact: memory is **128 MB on both free and paid** — paid buys CPU + subrequests, **not** memory. | No rate change. Truth-in-docs correction. Whether a host can *sustain* the workload ceiling depends on its maintenance throughput; benching that (and any resulting per-host maintenance profile) is a tracked follow-up. |
| 2026-06-15 | **Provider rate and limit re-check.** Official Cloudflare R2, Workers pricing, Workers limits, R2 consistency, AWS S3 consistency, and Cloudflare Access service-token docs were re-read during the pre-launch docs pass. Published R2 Standard rates and free-tier caps still match the cost model; Workers limits still show 10 ms CPU / 50 subrequests on Free, 30 s default / 5 min max CPU and 10,000 subrequests on Paid, and 128 MB memory on both. Cloudflare Access service-token verification examples were updated to use `CF-Access-Client-Id` + `CF-Access-Client-Secret`; the Worker verifier still consumes the Access JWT injected as `Cf-Access-Jwt-Assertion`. | No baerly-storage rate or ceiling change. Docs-only correction to auth verification and review dates. |
| 2026-06-22 | **R2, Workers, and S3 core rates re-verified; no change.** Official Cloudflare R2 pricing, Workers pricing, Workers limits, and AWS S3 standard pricing pages re-read. All per-op and per-GB rates, free-tier caps, and CPU/memory limits match the published cost model. | No rate change. Re-verification only. |
| 2026-06-22 | **Workers paid subrequest ceiling surfaced in graduation.md: 10,000/request (default; raisable to 10M; effective 2026-02-11).** Cloudflare raised the Workers paid default subrequest limit to 10,000/request as of 2026-02-11, with the cap configurable up to 10M. The 2026-06-15 re-check above already recorded this value in the cost model; `graduation.md`'s per-tier table and "How to raise a limit" table previously omitted the paid subrequest ceiling and were updated to state it explicitly (10,000 default; raisable to 10M; changed 2026-02-11). The free-tier wall (50 subrequests/request) is unchanged. | No baerly-storage code change. The in-band fold/GC slice reasoning that defends the free 50-subrequest wall is unaffected. |
| 2026-06-22 | **AWS S3 12-month free tier retired for new accounts (credit-based since 2025).** AWS replaced the 12-month S3 free tier for new accounts with a credit-based system. The self-hosted-Node "idle is free on S3" claim in `cost-model.md` now holds only for existing or paid accounts. Added a note to the rates table in `cost-model.md`. | Affects new self-hosted-Node deployments against fresh AWS accounts; existing accounts and paid plans are unchanged. |
| 2026-06-22 | **Effective Class A write-amplification corrected: 2-op commit floor → measured ~3× (R2/Cloudflare) / ~4× (S3/Node).** The commit path is two Class A ops (content PUT + `log/<seq>` create), but in-band maintenance (folds + GC) triggered on the write path raises the effective figure to ~3× on CF and ~4× on Node. Measured empirically via `pnpm bench:amortized-write-cost`; baseline at `docs/spec/attachments/amortized-write-cost-baseline.json`; gated by `tests/integration/write-amp.test.ts` (Task 3). The `baerly cost` CLI projection (Task 4) now uses the measured effective write-amp (r2=3, s3=4) rather than the commit floor. The 50M Class A/mo graduation trigger at ~3× corresponds to ≈ **390 writes/min** (was ≈580 at 2×). | Affects `baerly cost` projections and graduation arithmetic. No protocol change; `DeleteObject` (the GC sweep) is $0 on R2/S3 and excluded from the billable count. |
| 2026-06-22 | **`effective write-amp > 6` graduation trigger retired.** That trigger was calibrated against the old assumed 2-op commit floor (a 3× headroom meaning "maintenance badly behind"). With the effective write-amp now measured at ~3× / ~4× — and _stress_-measured to peak at ~4.0× even under pathological churn (`pnpm bench:write-amp-stress`, baseline `docs/spec/attachments/amortized-write-cost-stress-baseline.json`) — a sustained > 6 is unreachable through the bounded in-band maintenance path; the only route past ~4× is a CAS-retry storm, already governed by the per-collection ~30 writes/min throughput ceiling. The cost lines (`cost-model.md`, `graduation.md`) now name two cost triggers (Class A > 50M/mo, stored > 5 GB — _superseded 2026-06-23: storage is a cost signal at the ~10 GB R2 free-tier line, not a hard trigger_) plus the two capacity ceilings, and point at `db.compaction.deferred_total` / the defer `console.warn` as the "maintenance falling behind" signal. The two distinct "write-amp" quantities (compaction write-amp ~2× vs effective Class-A write-amp ~3–4×) are now disambiguated in `graduation.md`. | No rate change; graduation-trigger recalibration + docs disambiguation. Removes a trigger that could never fire against the measured baseline. |
| 2026-06-22 | **Theoretical cost-curve model published in `cost-model.md`.** Added an explicit $/mo curve section with formulas for both R2 and S3 paths (R2: `$5 floor + max(0, W×3 − 1M)·$4.50/1M + reads + storage`; S3: `W×4·$5/1M + reads + storage`, no free tier), a cost-vs-scale table at 1/10/30/100/390/1000 writes/min, and the M-size (~30 writes/min) breakdown: **~$18/mo on R2, ~$26/mo on S3** (write ops only). Research anchors: 50M Class A/mo ≈ $228 R2 / ~$337 S3 (_R2 figure corrected 2026-06-23 → ~$220, object-storage ops only_); S3 ~30–35% costlier for writes. No code change; no threshold change. Docs-only model surface. | Provides a defensible $/mo figure for the M-size operating point and an ops-vs-cost framing (object storage = zero-ops/no-on-call; a managed DB trades dollars for operational burden). Task 4's "Scale at a glance" cost row reuses the M-size figure (~$18/mo R2). |
| 2026-06-22 | **Advisory graduation threshold added: `GRADUATION_ADVISORY_CLASS_A_PER_MONTH = 13_000_000` (~100 writes/min, ~$58/mo R2 / ~$86/mo S3).** Owner-approved advisory line at 13M Class A/mo. `baerly cost` prints an eyes-open advisory note when the projected Class A rate crosses this threshold but stays under the 50M/mo hard trigger. Advisory is explicitly NOT a hard stop — it surfaces the ops-vs-cost tradeoff at the point where managed Postgres / D1 become comparably priced: object storage buys zero ops / no on-call / no migration; a managed DB trades those dollars for a schema, SQL, and an ops surface. The 50M/mo hard graduation trigger is unchanged. | New advisory signal in `baerly cost` output above 100 writes/min / ~$58/mo R2 / ~$86/mo S3. No protocol change; no rate change; no change to the 50M/mo hard trigger. |
| 2026-06-22 | **10 GB/tenant reframed as cost line; ~100 collections/tenant re-derived via bench; provenance bug fixed.** (1) `>10 GB/tenant stored` is the R2 free-tier storage boundary — a cost signal, not a protocol ceiling. Nothing in baerly-storage reads, enforces, or computes per-tenant byte totals; a tenant is a key prefix. Reframed across `graduation.md`, `thesis.md`, `sync-protocol.md`, `workload-fit.md`, `README.md`, `operations.md`, four example AGENTS.md. (2) `~100 collections/tenant` is now bench-grounded: `pnpm bench:collection-fanout` measures `admin usage` sweep cost as ≈ N × (1 LIST + up to 120 GETs per collection) — strictly linear. ~100 is the range where sweep cost becomes noticeable; nothing in the protocol enforces a cap (erosion, not a cliff). Baseline at `docs/spec/attachments/collection-fanout-baseline.json`. (3) Provenance bug fixed: `graduation.md` previously claimed both 10 GB/tenant and ~100 collections/tenant were "cited in code at `usage.ts:7`" — false. Neither is a code constant. The JSDoc at `usage.ts:7` cites only the 30-writes/min figure (`M_SIZE_WRITES_PER_MIN_PER_COLLECTION = 30` at line 66); the corrected provenance note names `cost-model.md`, `pricing-log.md`, and the fan-out bench as the authoritative sources. (4) Added "Scale at a glance" section to `workload-fit.md` with a builder-facing 6-row table; CPU/throughput walls marked as model/estimate pending real-infra measurement. (5) `BAERLY_MAINTENANCE_PROFILE=cf-paid` added to the `graduation.md` ops env vars table and the per-tier table (Cloudflare paid row). | No rate change. Capacity semantics clarification + bench-grounded fan-out re-derivation + docs cross-consistency pass. |
| 2026-06-23 | **Advisory threshold re-keyed to a write RATE (provider-agnostic); R2 cost figures scoped to object-storage ops only; 5 GB storage hard-trigger language removed.** (1) The advisory constant `GRADUATION_ADVISORY_CLASS_A_PER_MONTH = 13_000_000` became `GRADUATION_ADVISORY_WRITES_PER_MIN = 100`. The previous absolute 13M-Class-A threshold was R2-derived (100 writes/min × 3) and, applied to S3 (×4), fired at ~75 writes/min (~$65/mo) while the rendered note quoted the 100-writes/min figure (~$86) — provider-inconsistent. `percentOfAdvisory` now equals `writesPerMin / 100 × 100`, so the advisory fires at the same ~100 writes/min on every provider. On R2 that is ~13M Class A/mo, on S3 ~17.3M. (2) The R2 `$/mo` figures in `cost-model.md` / `graduation.md` and the `baerly cost` advisory note were scoped to **object-storage ops only** (matching what the CLI projects, which never added the floor): R2 advisory ~$58→**~$54**, 50M-trigger ~$228→**~$220**, M-size cost-curve row ~$58→**~$54** / 390-row ~$228→**~$223** / 1000-row ~$584→**~$579**. The $5/mo Workers Paid platform floor is now documented as a separate Cloudflare platform fee (absent on self-hosted Node / R2-over-S3-API / Workers free tier), with the all-in M-size figure (~$18/mo R2) shown as object-storage ops (~$13) + floor ($5). (3) The unwired "stored data > 5 GB hard trigger" language was removed; storage graduation is now consistently a **cost signal at the ~10 GB R2 free-tier line**, not a hard stop (the tooling enforces neither). | No upstream rate change. Corrects a provider-inconsistent advisory firing point and a docs/CLI dollar mismatch ($5 Workers Paid platform floor was in the docs' R2 figures but never in the CLI projection). The 50M Class A/mo hard trigger is unchanged. |
